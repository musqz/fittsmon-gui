# Maintainer: Eric Bakker <musqz at mf dot com>
pkgname=fittsmon-gui
pkgver=1.0.1
pkgrel=1
pkgdesc="Lightweight GTK3 GUI to configure fittsmon screen-corner hotspots"
arch=('any')
url="https://github.com/musqz/fittsmon-gui"
license=('MIT')
depends=('python' 'python-gobject' 'gtk3')
optdepends=(
    'fittsmon: screen-corner hotspot daemon this GUI configures'
    'arandr: set primary monitor (required for correct config file detection)'
)
source=("${pkgname}-${pkgver}.tar.gz::${url}/archive/refs/tags/v${pkgver}.tar.gz")
sha256sums=('8d7f87554a839395b682d3b2b009e89952a788450e523ae4670d99c13d9f440b')

package() {
    cd "${srcdir}/${pkgname}-${pkgver}"
    make install DESTDIR="${pkgdir}" PREFIX=/usr
}
